<aura:component >
	<aura:attribute name="numblist" type="list"/>
    <ui:button label="Diaplay number list" press="{!c.getNumber}"	/>
    <aura:iteration var="num" items="{!v.numblist}">
    <br></br>{!num.value}<br></br>
    </aura:iteration>
</aura:component>